## Build & Run

1. Install composer dependencies
   ```shell
   composer i
   ```
2. Start sail
    ```shell
    ./vendor/bin/sail up -d
    ```
3. Run migrations
   ```shell
    ./vendor/bin/sail artisan migrate
   ```

